<p style="color: green"><%= notice %></p>

<h1>Foods</h1>

<div id="foods">
  <% @foods.each do |food| %>
    <%= render food %>
    <p>
      <%= link_to "Open Food", user_food_path(@user, food), class: 'button-2' %>
    </p>
  <% end %>
</div>

<%= link_to "New food", new_user_food_path %>
