<p style="color: green"><%= notice %></p>

<h1>Recipes</h1>

<div id="recipes">
  <% @recipes.each do |recipe| %>
    <%= render recipe %>
    <p>
      <%= link_to "Open Recipe", user_recipe_path(@user, recipe), class: 'button-2' %>
    </p>
  <% end %>
</div>

<%= link_to "New recipe", new_user_recipe_path %>
